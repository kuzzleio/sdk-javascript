name: server#now
description: Fetch the current server timestamp, in Epoch-millis format.
hooks:
  before:
  after:
template: default
expected: ^(Epoch-millis timestamp:) [0-9]{13}$

sdk: js
version: 6
