name: ESLint
description: Run ESLint
runs:
  using: "composite"
  steps:
    - name: Install deps
      run: npm install && ls -l node_modules/
      shell: bash
    - name: Run lint
      run: npm run test:lint
      shell: bash
