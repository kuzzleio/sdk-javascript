name: ms#zrevrank
description: Returns the position of an element in a sorted set, in reverse order
hooks:
  before: curl -X POST kuzzle:7512/ms/_flushdb
  after:
template: default
expected:
  - ^0$
  - ^null$

sdk: js
version: 6
